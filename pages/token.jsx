import * as React from 'react';
import TokenMain from '../components/TokenMain';

const Token = ({props}) => {
  console.log("props are", props);

  return (
    <TokenMain pathId={null} />
  )
}

export default Token
