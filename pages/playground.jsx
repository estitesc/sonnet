import * as React from 'react';
import PlayMain from '../components/PlayMain';

const Home = ({props}) => {
  console.log("props are", props);

  return (
    <PlayMain pathId={null} />
  )
}

export default Home
