import * as React from 'react';
import Main from '../components/Main';

const Home = ({props}) => {
  console.log("props are", props);

  return (
    <Main pathId={null} />
  )
}

export default Home
